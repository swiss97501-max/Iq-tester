# .env.example — ใส่ key เท่านั้น ไม่ใส่ค่าจริง
# ตัวอย่าง DATABASE_URL สำหรับ PostgreSQL:
# postgres://USER:PASSWORD@HOST:PORT/DBNAME

DATABASE_URL=
# ถ้าต้องให้ client อ่านค่าบางตัว (Next.js) ให้ขึ้นต้นด้วย NEXT_PUBLIC_
NEXT_PUBLIC_API_URL=
JWT_SECRET=
NODE_ENV=development
